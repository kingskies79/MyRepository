import { CcMotoHoverDirective } from './cc-moto-hover.directive';

describe('CcMotoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CcMotoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
