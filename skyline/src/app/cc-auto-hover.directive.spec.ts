import { CcAutoHoverDirective } from './cc-auto-hover.directive';

describe('CcAutoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CcAutoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
